# 开发规范

- 注意 i18n、浅色/深色模式和响应式适配。
- 文件过大或逻辑复杂时及时拆分组件；样式按文件、组件和功能合理拆分，不要全部堆到 `styles.css`。
- 统一代码和样式风格，优先复用现有组件、变量和公共样式，不要随意增加 hover 效果。
- 结合项目上下文实现，清理确定无用的代码、样式和依赖，但不能影响正常功能。
- UI 修改必须检查实际效果。
- 添加必要的中文注释，不需要额外加“中文：”前缀。
- 用户可能同时开启多个 Codex 窗口，避免覆盖或影响其他窗口的修改。
- 遵循最小影响原则，不随意修改无关功能。
- 新增弹窗默认遵循项目标准 `AppModal` 规范，除非用户另有要求。
- 完成修改后检查代码与正常功能，最后简短总结。

## 临时文件管理

- 一次性的审计脚本、复现页面、测试配置、截图、日志和调试结果必须放到项目外的系统临时目录或任务产物目录，不得在仓库内新建或保留 `.tmp-*` 等临时目录；被 `.gitignore` 忽略也不代表可以留在项目中。
- 临时文件使用当前任务独立目录，任务结束后清理不再需要的临时脚本和调试材料；不要删除其他窗口仍在使用的文件。
- 有长期价值的回归测试应整理进现有正式测试目录。保留的审计文档记录结论和验证方式，不依赖已删除的临时脚本或本机临时路径。
- 提交或交付前检查新增文件和文档链接，确认没有遗留无用临时材料。正常构建产物、依赖缓存和正式项目配置按原有项目约定处理。
